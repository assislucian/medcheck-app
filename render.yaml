services:
  # Backend API - Deploy mínimo sem pandas (funcional)
  - type: web
    name: medcheck-backend
    env: python
    plan: starter
    buildCommand: |
      # Forçar Python 3.10 explicitamente
      echo "Tentando forçar Python 3.10..."
      
      # Mostrar versão atual
      python --version
      
      # Upgrade pip e build tools
      pip install --upgrade pip setuptools wheel
      
      # Instalar requirements (SEM pandas)
      pip install -r requirements.txt
      
      # Verificar instalação básica
      python -c "import fastapi, httpx; print('✅ FastAPI + HTTP OK')"
      
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port $PORT --workers 1
    
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.0"
      - key: ENVIRONMENT
        value: production
      - key: DEBUG
        value: false
      - key: CORS_ALLOWED_ORIGINS
        value: "*"
      - key: PYTHONPATH
        value: /opt/render/project/src
    
    healthCheckPath: /health
    
    # Garantir que ignora frontend
    buildFilter:
      paths:
      - "**"
      ignoredPaths:
      - "frontend/**"
      - "*.js"
      - "*.ts"
      - "*.tsx"
      - "package.json"
      - "node_modules/**"
