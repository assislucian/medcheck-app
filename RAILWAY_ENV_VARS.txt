# RAILWAY ENVIRONMENT VARIABLES
# Copie estas variáveis para o Railway Dashboard

# 1. DATABASE_URL (será gerada automaticamente pelo Railway PostgreSQL)
DATABASE_URL=postgresql://postgres:password@hostname:port/railway

# 2. JWT SECRET (gere uma nova chave)  
JWT_SECRET=W6YCfUhN/ePyYZMohuBKXfYgvPRLhu2ioN7kQ4u3p40=

# 3. FRONTEND ORIGINS (adicione seu domínio Vercel)
FRONTEND_ORIGINS=https://medcheck-app-vercel.vercel.app,http://localhost:5173

# 4. NODE ENVIRONMENT
NODE_ENV=production

# 5. PYTHON PATH
PYTHONPATH=/app

# 6. CORS ALLOWED ORIGINS (fallback)
CORS_ALLOWED_ORIGINS=https://medcheck-app-vercel.vercel.app,http://localhost:5173

# =====================================
# VERCEL ENVIRONMENT VARIABLES  
# Copie esta variável para o Vercel Dashboard
# =====================================

# 1. API URL (aponte para seu Railway backend)
VITE_API_URL=https://medcheck-backend-production.up.railway.app

# =====================================
# COMO GERAR JWT_SECRET SEGURO
# =====================================
# Execute no terminal:
# openssl rand -base64 32
# 
# Exemplo:
# JWT_SECRET=j78Slcfb7grTthQwk9048U35BcfZ0CjF6pYKWvwThTM=